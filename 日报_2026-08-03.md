# 工作日报 —— 2026年8月3日

## 一、ExcelOperationSkill Matcher 复用 Bug

**问题**：`ExcelOperationSkill.java` 第 71 行和第 76 行对同一个 `Matcher` 对象调了两次 `find()`，中间没有 `reset()`。`Matcher.find()` 有状态，第一次消费匹配后指针移动，导致"修改第N行"指令永远匹配不到行号。

**位置**：`src/main/java/com/clawbot/wechatbot/feature/excel/skill/ExcelOperationSkill.java:71-78`

**修复**：第二次 `find()` 前加 `rowMatcher.reset()`，或用变量保存匹配结果复用。

---

## 二、B站搜索完全失效（重点）

**现象**：搜索"凡人修仙传"、"斗罗大陆"等任何关键词，均返回「没有在B站找到与"XXX"相关的作品。」

**根因**：B站 `search/type` 接口（`api.bilibili.com/x/web-interface/search/type`）已对服务器 IP 实施风控拦截，返回 HTML 反爬页面（"出错啦! - aba.bilibili.com"）而非 JSON。代码链路中两处缺陷叠加导致静默失败：

| 环节 | 文件 | 问题 |
|------|------|------|
| HTTP 拦截检测 | `BilibiliHttpClient.java:126-131` | `isPermissionLimitedBody()` 只匹配 JSON 错误码（-412/-352/-403），不识别 HTML 拦截页 |
| JSON 解析 | `BilibiliPageParser.java:68-101` | `parseSearchMediaJson()` 中 `readTree()` 对 HTML 抛异常，被 `catch(Exception ignored)` 吞掉，返回空列表 |
| 搜索入口 | `PublicPageBilibiliSource.java:42-43` | 使用已被风控的 `search/type` 接口 |

完整链路：
```
搜索请求 → search/type 接口 → 200 + HTML拦截页
  → isPermissionLimitedBody 漏判 → HTML进JSON解析器
  → readTree抛异常 → catch吞掉 → 返空列表
  → "没有在B站找到相关作品"
```

**实测验证**：
- `search/type` → HTML 拦截页
- `search/all/v2` → 正常返回 JSON（已验证"凡人修仙传"等均可搜到）
- `x/web-interface/view`、`nav` 等其它接口正常

**修复方案**：
1. 将搜索接口从 `search/type` 切换为 `search/all/v2`，适配其 `data.result[].data[]` 嵌套结构
2. 增强 `isPermissionLimitedBody()`，加入 HTML 拦截页识别（含 `"出错啦"` 字样或 Content-Type 非 application/json）
3. `parseSearchMediaJson()` 异常分支加日志，避免静默吞错

---

## 三、待处理

- [ ] 修复 ExcelOperationSkill Matcher reset 问题
- [ ] 修复 B站搜索接口 → 切换到 search/all/v2
- [ ] 增强 BilibiliHttpClient 拦截页检测
- [ ] BilibiliPageParser 解析异常加日志
